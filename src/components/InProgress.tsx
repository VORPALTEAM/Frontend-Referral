import React, {useState} from 'react'

const ProgressSection = ({ message  }) => {

    return(
        <div className="Progress--section">
            <h2>{message}</h2>
        </div>
    )
}

export default ProgressSection